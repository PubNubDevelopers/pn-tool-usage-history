# PubNub Internal Admin Framework - Backend Configuration
# Copy this file to .env and configure as needed

# ============================================================================
# Server Configuration
# ============================================================================

# Server port (default: 5050)
# The Express server will listen on this port
PORT=5050

# ============================================================================
# Internal Admin API Configuration
# ============================================================================

# Internal Admin API URL (DO NOT CHANGE - requires VPN access)
# This endpoint is only accessible within the PubNub VPN
INTERNAL_ADMIN_URL=https://internal-admin.pubnub.com

# ============================================================================
# Notes
# ============================================================================

# 1. VPN REQUIRED: You must be connected to PubNub VPN to access internal-admin.pubnub.com
# 2. No Additional Configuration: All authentication happens via session tokens obtained at login
# 3. No Credentials Here: Never store admin credentials in environment variables
# 4. Local Development: Default values work for local development - no .env file needed
# 5. Production: Follow internal deployment procedures for production configuration

# ============================================================================
# Environment Variables NOT Used (documented for reference)
# ============================================================================

# The following are NOT used but documented for awareness:
# - SESSION_SECRET: Not needed (session tokens from internal-admin API)
# - DATABASE_URL: Not needed (no database, all data from API)
# - API_KEY: Not needed (admin credentials provided at login)
# - ADMIN_EMAIL/ADMIN_PASSWORD: NEVER store credentials in .env files
